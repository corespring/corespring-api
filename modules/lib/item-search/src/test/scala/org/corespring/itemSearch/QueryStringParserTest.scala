package org.corespring.itemSearch

import org.specs2.mutable.Specification

class QueryStringParserTest extends Specification {

  "scopedSearchQuery" should {
    "return an error if the query is invalid json" in pending
    "return an error if the query can't be parsed as an item index query" in pending
    "return the scoped query" in pending
  }
}
